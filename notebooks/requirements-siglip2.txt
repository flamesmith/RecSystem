# Reproducible local environment for siglip2_product_exploration.ipynb.
#
# This project is currently running on an Intel (x86_64) Mac. PyTorch stopped
# publishing newer Intel-macOS wheels after the 2.2 release line, so torch is
# intentionally pinned to 2.2.2 and NumPy stays on the compatible 1.x ABI.
torch==2.2.2
numpy==1.26.4
transformers==4.57.6
pandas==2.2.3
Pillow==11.3.0
matplotlib==3.10.6
scikit-learn==1.7.2
tqdm==4.67.1
ipykernel==6.30.1
ipywidgets==8.1.7
nbconvert==7.16.6
